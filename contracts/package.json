{
  "name": "soul-marketplace-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Soul Marketplace - autonomous agent survival",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run deploy.js --network hardhat",
    "deploy:testnet": "hardhat run deploy.js --network baseSepolia",
    "deploy:mainnet": "hardhat run deploy.js --network base",
    "verify": "hardhat verify",
    "node": "hardhat node"
  },
  "dependencies": {
    "@coinbase/cdp-sdk": "^1.44.1",
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.28.6"
  }
}
